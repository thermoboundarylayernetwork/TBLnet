# Core dependencies

pandas>=1.3
numpy>=1.21
scikit-learn>=1.0

# Utilities / optional
matplotlib>=3.4
seaborn>=0.11
tqdm>=4.60
joblib>=1.1

# PyTorch (CPU-only supported; if installation fails, follow the official PyTorch install command for your platform)
torch